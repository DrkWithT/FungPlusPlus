# test07.fung #

use stdio
use stringify

fun doFib(val n,)
    if n <= 0
        ret 0
    end

    if n == 1
        ret 1
    end

    let x = @ doFib(n - 1)
    let y = @ doFib(n - 2)

    ret x + y
end

fun main()
    print(toString(doFib(5)))
    ret $T
end
