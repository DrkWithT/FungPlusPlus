# test06.fung #

use stdio
use stringify

fun findTriMax(val a, val b, val c)
    if a >= b && a >= c
        ret a
    else if b >= a && b >= c
        ret b
    else
        ret c
    end end
end

print(toString(findMaxInNums(1, 2, 3)))
